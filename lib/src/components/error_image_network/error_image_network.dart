import 'package:flutter/material.dart';

class ErrorImageNetwork extends StatelessWidget {
  const ErrorImageNetwork({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const Text('Error');
  }
}
